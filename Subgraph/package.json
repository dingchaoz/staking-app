{
  "name": "Staking-App",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ dingchaoz/Staking-App",
    "create-local": "graph create --node http://localhost:8020/ dingchaoz/Staking-App",
    "remove-local": "graph remove --node http://localhost:8020/ dingchaoz/Staking-App",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dingchaoz/Staking-App"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
